import projectConfig from '/pagic.config.js';
import Ga from '/_ga.js';
var _a, _b;
export default {
    config: { "root": "/", ...projectConfig, ...(_b = (_a = projectConfig.i18n) === null || _a === void 0 ? void 0 : _a.overrides) === null || _b === void 0 ? void 0 : _b['en'], branch: 'master' },
    'pagePath': "blog/design_pagic_config_ts.md",
    'layoutPath': "blog/_layout.tsx",
    'outputPath': "blog/design_pagic_config_ts.html",
    'title': "Design pagic.config.ts",
    'content': React.createElement("article", { dangerouslySetInnerHTML: {
            __html: '<h1>Design pagic.config.ts</h1>\n<blockquote>\n<p>Part of the content in this article may be out of date. Visit <a href="../docs/config.html">Config</a> to view the latest configuration fields.</p>\n</blockquote>\n<p>As a <del>senior markdown engineer</del> senior blogger, I am passionate about <del>blogging</del> to develop blog systems and have written many blog themes.</p>\n<p>Finally, I couldn\'t be satisfied with writing themes, and I started writing a blog system.</p>\n<p>Or in a more general sense, a static website generator.</p>\n<p>Now <a href="https://github.com/xcatliu/pagic">Pagic</a> has completed a prototype, I also invited some friends to try it out, after some iterations, I decided to redesign <code>pagic.config.ts</code>, after all, as a static website generator, most users only need to configure the <code>pagic.config.ts</code> to build a website, so the design of the configuration file is very important.</p>\n<h2 id="design-principles">Design principles<a class="anchor" href="#design-principles">§</a></h2>\n<ol>\n<li><a href="https://en.wikipedia.org/wiki/Convention_over_configuration">Convention over configuration</a></li>\n<li>As semantically as possible, clear at a glance</li>\n<li>The type is uniform, no options can pass both strings and functions</li>\n<li>Reference: <a href="https://deno.land/manual/contributing/style_guide">Deno</a>, <a href="https://www.typescriptlang.org/docs/handbook/tsconfig-json.html">tsconfig.json</a>, <a href="https://webpack.js.org/configuration/">webpack</a>, <a href="https://hexo.io/zh-cn/docs/configuration">Hexo</a>, <a href="https://vuepress.vuejs.org/config/">VuePress</a>, <a href="https://gohugo.io/getting-started/configuration/">Hugo</a></li>\n</ol>\n<h2 id="naming-convention">Naming convention<a class="anchor" href="#naming-convention">§</a></h2>\n<h3 id="file-name-and-directory-name">File name and directory name<a class="anchor" href="#file-name-and-directory-name">§</a></h3>\n<p>Use underscore naming convention <code>foo_bar/baz_v2.md</code>。</p>\n<p>In accordance with: <a href="https://deno.land/manual/contributing/style_guide#use-underscores-not-dashes-in-filenames">Deno style guide</a></p>\n<h3 id="variables-and-parameters">Variables and parameters<a class="anchor" href="#variables-and-parameters">§</a></h3>\n<p>Use camel case naming convention <code>fooBar</code>。</p>\n<p>In accordance with: <a href="https://deno.land/manual/contributing/style_guide#exported-functions-max-2-args-put-the-rest-into-an-options-object">Deno style guide</a></p>\n<h3 id="config-file-name">Config file name<a class="anchor" href="#config-file-name">§</a></h3>\n<p><code>pagic.config.tsx</code> or <code>pagic.config.ts</code>.</p>\n<ul>\n<li>The priority of <code>pagic.config.tsx</code> is higher (In accordance with: <a href="https://webpack.js.org/configuration/resolve/#resolveextensions">webpack default loading order</a>)</li>\n<li>Why provide <code>tsx</code> and <code>ts</code>? Why not unified into <code>ts</code>? Because once the jsx syntax is used, it must be named <code>tsx</code>, which is a limitation of TypeScript</li>\n<li>Why not unified into <code>tsx</code>? Because in most cases, using <code>ts</code> is sufficient</li>\n<li>Only find the configuration file in the current running directory, <strong>does not provide</strong> <code>--config</code> to specify the configuration file (convention over configuration)</li>\n<li><code>json</code> format and <code>yaml</code> format have many restrictions, so use <code>ts</code> to write configuration files, <strong>not provide</strong> other options (convention over configuration)</li>\n<li>Among the configuration files of other static website generators, the most confusing thing is that the theme configuration is separated out, which makes me often need to hesitate whether a config item belongs to the main configuration file or the theme config file. So, there is only one config file in Pagic</li>\n</ul>\n<p>In accordance with:</p>\n<ul>\n<li><code>tsconfig.json</code>（ts）</li>\n<li><code>webpack.config.js</code>（webpack）</li>\n<li><code>.vuepress/config.js</code>（VuePress）</li>\n<li><code>_config.yml</code>（Hexo）</li>\n<li><code>config.json</code>（Hugo）</li>\n</ul>\n<h2 id="file-related-configuration">File related configuration<a class="anchor" href="#file-related-configuration">§</a></h2>\n<p>For file related configuration, refer to <code>tsconfig.json</code>.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>srcDir</code></td>\n<td><code>string</code></td>\n<td><code>\'.\'</code></td>\n<td>The source directory of the build. All of the pages will be built into <code>outDir</code> with the same directory structure. The default is the current directory, at this time the <code>README.md</code> in the current directory will be built into <code>${outDir}/index.html</code></td>\n</tr>\n<tr>\n<td><code>outDir</code></td>\n<td><code>string</code></td>\n<td><code>\'dist\'</code></td>\n<td>Output directory</td>\n</tr>\n<tr>\n<td><code>include</code></td>\n<td><code>glob[]</code></td>\n<td><code>undefined</code></td>\n<td><code>include</code> limits the files that will be scanned in <code>srcDir</code>. It is often used to specify some files or directories when <code>srcDir</code> is configured as <code>.</code>. glob syntax <a href="https://github.com/isaacs/minimatch">see here</a></td>\n</tr>\n<tr>\n<td><code>exclude</code></td>\n<td><code>glob[]</code></td>\n<td>Follow up in detail</td>\n<td><code>exclude</code> is similar to <code>include</code>, used to exclude the specified file in <code>srcDir</code></td>\n</tr>\n<tr>\n<td><code>root</code></td>\n<td><code>string</code></td>\n<td><code>\'/\'</code></td>\n<td>The base path of the deployment site. If the website is deployed in a sub-path, such as <code>https://xcatliu.github.io/pagic/</code>, then <code>root</code> should be set to <code>\'/pagic/\'</code></td>\n</tr>\n</tbody>\n</table></div>\n<h3 id="exclude"><code>exclude</code><a class="anchor" href="#exclude">§</a></h3>\n<p>The default value for <code>exclude</code> is long:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token punctuation">[</span>\n  <span class="token comment">// Dot files</span>\n  <span class="token string">\'**/.*\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// Node common files</span>\n  <span class="token string">\'**/package.json\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/package-lock.json\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/node_modules\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'pagic.config.ts\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'pagic.config.tsx\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// <a class="token url-link" href="https://docs.npmjs.com/using-npm/developers.html#keeping-files-out-of-your-package">https://docs.npmjs.com/using-npm/developers.html#keeping-files-out-of-your-package</a></span>\n  <span class="token string">\'**/config.gypi\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/CVS\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/npm-debug.log\'</span><span class="token punctuation">,</span>\n\n  <span class="token comment">// ${config.outDir} will be added later</span>\n<span class="token punctuation">]</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Note that each item here can be matched to a file or directory. Taking <code>**/node_modules</code> as an example, it:</p>\n<ul>\n<li>Not only can it match the <code>node_modules</code> directory under <code>${srcDir}</code> (and all files in this directory)</li>\n<li>It can also match the <code>node_modules</code> file under <code>${srcDir}</code></li>\n<li>Of course, it can also match the <code>node_modules</code> directory or file in any multi-level subdirectory under <code>${srcDir}</code></li>\n</ul>\n<p>In fact, <code>**/node_modules</code> will be converted to <code>**/node_modules{,/**}</code>, so that all the above situations can be matched.</p>\n<p>have to be aware of is:</p>\n<ul>\n<li>When matching, start with <code>srcDir</code> as the root directory. For example, <code>pagic.config.{ts,tsx}</code> can only match the files under <code>srcDir</code>, but not the files under the subdirectories of <code>srcDir</code>. To match the subdirectories, you need to add the prefix <code> */</code>, if you want to match any multi-level subdirectory, you need to add the prefix <code>**/</code></li>\n<li>The matching strategy of <code>include</code> is the same as that of <code>exclude</code></li>\n<li>User-configured <code>exclude</code> will append to the default value array instead of replacing it</li>\n</ul>\n<p>Through these configurations, Pagic can be flexibly used in various scenarios:</p>\n<h3 id="scenario-1-pure-website-separate-directory-to-store-source-files">Scenario 1: Pure website, separate directory to store source files<a class="anchor" href="#scenario-1-pure-website-separate-directory-to-store-source-files">§</a></h3>\n<p>If you just want to build a website, then the most convenient way is to use this directory structure to separate the source files from the built files:</p>\n<pre class="language-bash"><code class="language-bash">website/\n├── dist/\n<span class="token operator">|</span>   └── index.html\n├── src/\n<span class="token operator">|</span>   ├── _layout.tsx\n<span class="token operator">|</span>   └── README.md\n└── pagic.config.ts\n</code></pre>\n<p>At this time, the configuration of <code>pagic.config.ts</code> is very simple:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'src\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h3 id="scenario-2-pure-website-the-root-directory-stores-the-source-files">Scenario 2: Pure website, the root directory stores the source files<a class="anchor" href="#scenario-2-pure-website-the-root-directory-stores-the-source-files">§</a></h3>\n<p>There is usually a <code>README.md</code> in the root directory of the project. Sometimes we want this file to be constructed as a page. At this time, we can set <code>srcDir</code> to <code>\'.\'</code>, for example, refer to the directory structure of GitBook Is like this:</p>\n<pre class="language-bash"><code class="language-bash">book/\n├── dist/\n<span class="token operator">|</span>   ├── basics/\n<span class="token operator">|</span>   <span class="token operator">|</span>   └── index.html\n<span class="token operator">|</span>   ├── advenced/\n<span class="token operator">|</span>   <span class="token operator">|</span>   └── index.html\n<span class="token operator">|</span>   └── index.html\n├── basics/\n<span class="token operator">|</span>   └── README.md\n├── advenced/\n<span class="token operator">|</span>   └── README.md\n├── _layout.tsx\n├── README.md\n└── pagic.config.ts\n</code></pre>\n<p>At this time, the configuration of <code>pagic.config.ts</code> is also very simple:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Note that if there are files that need to be excluded, you can use <code>exclude</code> to exclude them, for example:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n  exclude<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'examples\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h3 id="scene-3-project--website">Scene 3: Project + Website<a class="anchor" href="#scene-3-project--website">§</a></h3>\n<p>If you want to build a website in a project and you don\'t want to create another repo, then these two methods can meet the needs:</p>\n<ol>\n<li>Place the website source files in the <code>docs</code> directory, and configure <code>srcDir</code> as <code>\'docs\'</code>. The advantage is simple configuration, no need to configure <code>include</code> and <code>exclude</code></li>\n<li>Build the website directly under the root directory, configure <code>srcDir</code> as <code>\'.\'</code>, and then configure <code>include</code> to include the storage directory of the website. The advantage is that it contains <code>README.md</code> in the root directory</li>\n</ol>\n<h3 id="scene-4-only-show-readmemd">Scene 4: Only show <a href="http://README.md">README.md</a><a class="anchor" href="#scene-4-only-show-readmemd">§</a></h3>\n<p>Some projects are very simple. You only need a <code>README.md</code> and no other pages are needed. At this time, you can configure <code>include</code> to include only <code>README.md</code>:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n  include<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'README.md\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h2 id="plugin">Plugin<a class="anchor" href="#plugin">§</a></h2>\n<p>Plugins are the core function of Pagic, and Pagic even splits the most basic build process into built-in plugins. There are three types of plugins:</p>\n<ol>\n<li>Built-in plugin: the most basic build process, it will run by default</li>\n<li>Official plugin: plugin implemented by Pagic, optional, such as: <code>sidebar</code>, <code>ga</code>, etc.</li>\n<li>Third-party plugins: plugins implemented by third parties, optional. Following Deno\'s design, the entrance is a url, such as: <code>https://github.com/xcatliu/pagic_plugin_example/blob/master/mod.ts</code></li>\n</ol>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>plugins</code></td>\n<td><code>string[]</code></td>\n<td><code>[\'clean\', \'init\', \'md\', \'tsx\', \'script\', \'layout\', \'out\']</code></td>\n<td>The plugin list, the value of the built-in plugin and the official plugin are the plugin name, and the value of the third-party plugin is its entrance url</td>\n</tr>\n</tbody>\n</table></div>\n<p>The order in which the plugins are executed is in the order of their configuration, unless the plugin is implemented with an insert attribute:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  name<span class="token operator">:</span> <span class="token string">\'customPlugin\'</span><span class="token punctuation">,</span>\n  insert<span class="token operator">:</span> <span class="token string">\'before:script\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// The design of fn refers to Deno <a class="token url-link" href="https://deno.land/manual/testing#writing-tests">https://deno.land/manual/testing#writing-tests</a></span>\n  <span class="token function-variable function">fn</span><span class="token operator">:</span> <span class="token punctuation">(</span>pagic<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>\n    <span class="token comment">// balabala</span>\n  <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p><code>name</code> is the unique identifier of the plugin, and it will be used to sort the plugins in <code>insert</code>.</p>\n<p>The syntax of <code>insert</code> is <code>before:${pluginName}</code> or <code>after:${pluginName}</code>. This method is more convenient and more flexible than other static website generators that need to register various life cycle hooks.</p>\n<p><code>fn</code> is a function that only accepts one parameter, an instance of <code>pagic</code>, which can access all the configurations and contexts of Pagic in operation. If the plugin requires some additional configuration, you can add a config item <code>customPlugin</code> in <code>pagic.config.ts</code>, and then you can get the configuration through <code>pagic.config.customPlugin</code> in <code>fn</code>.</p>\n<p>Have to be aware of is:</p>\n<ul>\n<li>User-configured <code>plugins</code> will be appended to the default array instead of replacing it</li>\n<li>When the plugin is running, it will be sorted according to its <code>insert</code> before running</li>\n<li>You can use the <code>-</code> prefix to delete a built-in plugin. This feature is not commonly used, and only needs to be used in the following two situations:\n<ol>\n<li>Just use React as a template engine. When you want to build an html page that does not load React, you can use <code>-script</code> to remove the plugin that builds js scripts. Note that scripts in React such as <code>useState</code> will not be available to execute on the client.</li>\n<li>Want to replace some built-in plugins by yourself, such as using <code>-md</code> to remove the plugin that parses the markdown file, and then add a custom plugin <code>https://github.com/xcatliu/pagic_plugin_custom_md/blob/master/mod.ts</code> This can modify Pagic\'s core construction process to achieve a higher degree of freedom of personalization</li>\n</ol>\n</li>\n<li>Third-party plugins generally require the <code>insert</code> attribute, otherwise it will run after <code>write</code>. Few plugins need to be executed after the build is completed and the file is written</li>\n</ul>\n<h2 id="themes">Themes<a class="anchor" href="#themes">§</a></h2>\n<p>Theme is one of the core functions in Pagic, there are two official themes and lots of third-party themes.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>theme</code></td>\n<td><code>string</code></td>\n<td><code>default</code></td>\n<td>The value of the official theme is the theme name, and the value of the third-party plugin is its entrance url</td>\n</tr>\n</tbody>\n</table></div>\n<p>The operating mechanism of the theme is easy to understand: when you run <code>pagic build</code>, all files in the theme will be &quot;copied&quot; to <code>srcDir</code>, and then run <code>build</code> in the normal mode.</p>\n<p>Of course, this &quot;copy&quot; does not actually copy the file. And when conflicting files are encountered, the user\'s files shall prevail.</p>\n<p>Why does the theme need an entry file?</p>\n<p>Because in Deno’s design, module are based on urls. Imagine a URL represents a theme. We can’t use <code>fs.readdir</code> to find all files in this theme directory like node, so there must be an entry file to indicate which files are included in this topic:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  files<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'assets/index.css\'</span><span class="token punctuation">,</span> <span class="token string">\'assets/reset.css\'</span><span class="token punctuation">,</span> <span class="token string">\'assets/variables.css\'</span><span class="token punctuation">,</span> <span class="token string">\'_layout.tsx\'</span><span class="token punctuation">,</span> <span class="token string">\'favicon.ico\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Among them, <code>files</code> means files that need to be &quot;copied&quot; to <code>srcDir</code>.</p>\n<h2 id="website-configuration">Website configuration<a class="anchor" href="#website-configuration">§</a></h2>\n<p>Here are some convential configurations, which are usually implemented by plugins or themes.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>title</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>The title of the website, usually placed after the title of the page, such as: <code>Config · Pagic</code>, if there is no page title on the page, only the website title <code>Pagic</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>Website Description, usually displayed in <code>&lt;head&gt;&lt;meta name=&quot;description&quot;&gt;</code>, and may also be displayed on the page</td>\n</tr>\n<tr>\n<td><code>head</code></td>\n<td><code>ReactElement</code></td>\n<td><code>undefined</code></td>\n<td>Additional content injected into <code>&lt;head&gt;</code>, can be written in jsx. Note that you need to change the configuration file to <code>pagic.config.tsx</code></td>\n</tr>\n<tr>\n<td><code>sidebar</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Sidebar</td>\n</tr>\n<tr>\n<td><code>nav</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Navigation on the top</td>\n</tr>\n<tr>\n<td><code>github</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>The GitHub address of the website, usually displayed in the upper right corner of the page</td>\n</tr>\n<tr>\n<td><code>ga</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Google Analytics configuration</td>\n</tr>\n<tr>\n<td><code>gitalk</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Gitalk configuration</td>\n</tr>\n<tr>\n<td><code>tocAd</code></td>\n<td><code>ReactNode</code></td>\n<td><code>undefined</code></td>\n<td>Ads displayed above toc</td>\n</tr>\n<tr>\n<td><code>tools</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Some extra features</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id="development-environment-config">Development environment config<a class="anchor" href="#development-environment-config">§</a></h2>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>watch</code></td>\n<td><code>boolean</code></td>\n<td><code>false</code></td>\n<td>Watch file changes to rebuild.</td>\n</tr>\n<tr>\n<td><code>serve</code></td>\n<td><code>boolean</code></td>\n<td><code>false</code></td>\n<td>Start local service, preview static website.</td>\n</tr>\n<tr>\n<td><code>port</code></td>\n<td><code>number</code></td>\n<td><code>8000</code></td>\n<td>Specify the port of the local service.</td>\n</tr>\n</tbody>\n</table></div>'
        } }),
    'head': React.createElement(React.Fragment, null,
        React.createElement(Ga, { id: "UA-45256157-16" }),
        React.createElement(React.Fragment, { key: ".1" },
            React.createElement(React.Fragment, { key: ".0" },
                React.createElement("script", { src: "/i18n.js", type: "module" })),
            React.createElement("link", { crossOrigin: "anonymous", href: "https://cdn.jsdelivr.net/npm/katex@0.12.0/dist/katex.min.css", integrity: "sha384-AfEj0r4/OFrOo5t7NnNe46zW/tFgW6x/bCJG8FqQCEo3+Aro6EYUG4+cU+KJWu/X", rel: "stylesheet" }))),
    'script': React.createElement(React.Fragment, null,
        React.createElement("script", { src: "https://cdn.pagic.org/react@17.0.2/umd/react.production.min.js" }),
        React.createElement("script", { src: "https://cdn.pagic.org/react-dom@17.0.2/umd/react-dom.production.min.js" }),
        React.createElement("script", { src: "/index.js", type: "module" })),
    'footer': React.createElement("footer", null,
        "Powered by\u00A0",
        React.createElement("a", { href: "https://github.com/xcatliu/pagic", target: "_blank" }, "Pagic")),
    'language': {
        "code": "en",
        "name": "English",
        "root": "/"
    },
    'contentTitle': React.createElement("h1", { key: "0" }, "Design pagic.config.ts"),
    'contentBody': React.createElement("article", { dangerouslySetInnerHTML: {
            __html: '<blockquote>\n<p>Part of the content in this article may be out of date. Visit <a href="../docs/config.html">Config</a> to view the latest configuration fields.</p>\n</blockquote>\n<p>As a <del>senior markdown engineer</del> senior blogger, I am passionate about <del>blogging</del> to develop blog systems and have written many blog themes.</p>\n<p>Finally, I couldn\'t be satisfied with writing themes, and I started writing a blog system.</p>\n<p>Or in a more general sense, a static website generator.</p>\n<p>Now <a href="https://github.com/xcatliu/pagic">Pagic</a> has completed a prototype, I also invited some friends to try it out, after some iterations, I decided to redesign <code>pagic.config.ts</code>, after all, as a static website generator, most users only need to configure the <code>pagic.config.ts</code> to build a website, so the design of the configuration file is very important.</p>\n<h2 id="design-principles">Design principles<a class="anchor" href="#design-principles">§</a></h2>\n<ol>\n<li><a href="https://en.wikipedia.org/wiki/Convention_over_configuration">Convention over configuration</a></li>\n<li>As semantically as possible, clear at a glance</li>\n<li>The type is uniform, no options can pass both strings and functions</li>\n<li>Reference: <a href="https://deno.land/manual/contributing/style_guide">Deno</a>, <a href="https://www.typescriptlang.org/docs/handbook/tsconfig-json.html">tsconfig.json</a>, <a href="https://webpack.js.org/configuration/">webpack</a>, <a href="https://hexo.io/zh-cn/docs/configuration">Hexo</a>, <a href="https://vuepress.vuejs.org/config/">VuePress</a>, <a href="https://gohugo.io/getting-started/configuration/">Hugo</a></li>\n</ol>\n<h2 id="naming-convention">Naming convention<a class="anchor" href="#naming-convention">§</a></h2>\n<h3 id="file-name-and-directory-name">File name and directory name<a class="anchor" href="#file-name-and-directory-name">§</a></h3>\n<p>Use underscore naming convention <code>foo_bar/baz_v2.md</code>。</p>\n<p>In accordance with: <a href="https://deno.land/manual/contributing/style_guide#use-underscores-not-dashes-in-filenames">Deno style guide</a></p>\n<h3 id="variables-and-parameters">Variables and parameters<a class="anchor" href="#variables-and-parameters">§</a></h3>\n<p>Use camel case naming convention <code>fooBar</code>。</p>\n<p>In accordance with: <a href="https://deno.land/manual/contributing/style_guide#exported-functions-max-2-args-put-the-rest-into-an-options-object">Deno style guide</a></p>\n<h3 id="config-file-name">Config file name<a class="anchor" href="#config-file-name">§</a></h3>\n<p><code>pagic.config.tsx</code> or <code>pagic.config.ts</code>.</p>\n<ul>\n<li>The priority of <code>pagic.config.tsx</code> is higher (In accordance with: <a href="https://webpack.js.org/configuration/resolve/#resolveextensions">webpack default loading order</a>)</li>\n<li>Why provide <code>tsx</code> and <code>ts</code>? Why not unified into <code>ts</code>? Because once the jsx syntax is used, it must be named <code>tsx</code>, which is a limitation of TypeScript</li>\n<li>Why not unified into <code>tsx</code>? Because in most cases, using <code>ts</code> is sufficient</li>\n<li>Only find the configuration file in the current running directory, <strong>does not provide</strong> <code>--config</code> to specify the configuration file (convention over configuration)</li>\n<li><code>json</code> format and <code>yaml</code> format have many restrictions, so use <code>ts</code> to write configuration files, <strong>not provide</strong> other options (convention over configuration)</li>\n<li>Among the configuration files of other static website generators, the most confusing thing is that the theme configuration is separated out, which makes me often need to hesitate whether a config item belongs to the main configuration file or the theme config file. So, there is only one config file in Pagic</li>\n</ul>\n<p>In accordance with:</p>\n<ul>\n<li><code>tsconfig.json</code>（ts）</li>\n<li><code>webpack.config.js</code>（webpack）</li>\n<li><code>.vuepress/config.js</code>（VuePress）</li>\n<li><code>_config.yml</code>（Hexo）</li>\n<li><code>config.json</code>（Hugo）</li>\n</ul>\n<h2 id="file-related-configuration">File related configuration<a class="anchor" href="#file-related-configuration">§</a></h2>\n<p>For file related configuration, refer to <code>tsconfig.json</code>.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>srcDir</code></td>\n<td><code>string</code></td>\n<td><code>\'.\'</code></td>\n<td>The source directory of the build. All of the pages will be built into <code>outDir</code> with the same directory structure. The default is the current directory, at this time the <code>README.md</code> in the current directory will be built into <code>${outDir}/index.html</code></td>\n</tr>\n<tr>\n<td><code>outDir</code></td>\n<td><code>string</code></td>\n<td><code>\'dist\'</code></td>\n<td>Output directory</td>\n</tr>\n<tr>\n<td><code>include</code></td>\n<td><code>glob[]</code></td>\n<td><code>undefined</code></td>\n<td><code>include</code> limits the files that will be scanned in <code>srcDir</code>. It is often used to specify some files or directories when <code>srcDir</code> is configured as <code>.</code>. glob syntax <a href="https://github.com/isaacs/minimatch">see here</a></td>\n</tr>\n<tr>\n<td><code>exclude</code></td>\n<td><code>glob[]</code></td>\n<td>Follow up in detail</td>\n<td><code>exclude</code> is similar to <code>include</code>, used to exclude the specified file in <code>srcDir</code></td>\n</tr>\n<tr>\n<td><code>root</code></td>\n<td><code>string</code></td>\n<td><code>\'/\'</code></td>\n<td>The base path of the deployment site. If the website is deployed in a sub-path, such as <code>https://xcatliu.github.io/pagic/</code>, then <code>root</code> should be set to <code>\'/pagic/\'</code></td>\n</tr>\n</tbody>\n</table></div>\n<h3 id="exclude"><code>exclude</code><a class="anchor" href="#exclude">§</a></h3>\n<p>The default value for <code>exclude</code> is long:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token punctuation">[</span>\n  <span class="token comment">// Dot files</span>\n  <span class="token string">\'**/.*\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// Node common files</span>\n  <span class="token string">\'**/package.json\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/package-lock.json\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/node_modules\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'pagic.config.ts\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'pagic.config.tsx\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// <a class="token url-link" href="https://docs.npmjs.com/using-npm/developers.html#keeping-files-out-of-your-package">https://docs.npmjs.com/using-npm/developers.html#keeping-files-out-of-your-package</a></span>\n  <span class="token string">\'**/config.gypi\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/CVS\'</span><span class="token punctuation">,</span>\n  <span class="token string">\'**/npm-debug.log\'</span><span class="token punctuation">,</span>\n\n  <span class="token comment">// ${config.outDir} will be added later</span>\n<span class="token punctuation">]</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Note that each item here can be matched to a file or directory. Taking <code>**/node_modules</code> as an example, it:</p>\n<ul>\n<li>Not only can it match the <code>node_modules</code> directory under <code>${srcDir}</code> (and all files in this directory)</li>\n<li>It can also match the <code>node_modules</code> file under <code>${srcDir}</code></li>\n<li>Of course, it can also match the <code>node_modules</code> directory or file in any multi-level subdirectory under <code>${srcDir}</code></li>\n</ul>\n<p>In fact, <code>**/node_modules</code> will be converted to <code>**/node_modules{,/**}</code>, so that all the above situations can be matched.</p>\n<p>have to be aware of is:</p>\n<ul>\n<li>When matching, start with <code>srcDir</code> as the root directory. For example, <code>pagic.config.{ts,tsx}</code> can only match the files under <code>srcDir</code>, but not the files under the subdirectories of <code>srcDir</code>. To match the subdirectories, you need to add the prefix <code> */</code>, if you want to match any multi-level subdirectory, you need to add the prefix <code>**/</code></li>\n<li>The matching strategy of <code>include</code> is the same as that of <code>exclude</code></li>\n<li>User-configured <code>exclude</code> will append to the default value array instead of replacing it</li>\n</ul>\n<p>Through these configurations, Pagic can be flexibly used in various scenarios:</p>\n<h3 id="scenario-1-pure-website-separate-directory-to-store-source-files">Scenario 1: Pure website, separate directory to store source files<a class="anchor" href="#scenario-1-pure-website-separate-directory-to-store-source-files">§</a></h3>\n<p>If you just want to build a website, then the most convenient way is to use this directory structure to separate the source files from the built files:</p>\n<pre class="language-bash"><code class="language-bash">website/\n├── dist/\n<span class="token operator">|</span>   └── index.html\n├── src/\n<span class="token operator">|</span>   ├── _layout.tsx\n<span class="token operator">|</span>   └── README.md\n└── pagic.config.ts\n</code></pre>\n<p>At this time, the configuration of <code>pagic.config.ts</code> is very simple:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'src\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h3 id="scenario-2-pure-website-the-root-directory-stores-the-source-files">Scenario 2: Pure website, the root directory stores the source files<a class="anchor" href="#scenario-2-pure-website-the-root-directory-stores-the-source-files">§</a></h3>\n<p>There is usually a <code>README.md</code> in the root directory of the project. Sometimes we want this file to be constructed as a page. At this time, we can set <code>srcDir</code> to <code>\'.\'</code>, for example, refer to the directory structure of GitBook Is like this:</p>\n<pre class="language-bash"><code class="language-bash">book/\n├── dist/\n<span class="token operator">|</span>   ├── basics/\n<span class="token operator">|</span>   <span class="token operator">|</span>   └── index.html\n<span class="token operator">|</span>   ├── advenced/\n<span class="token operator">|</span>   <span class="token operator">|</span>   └── index.html\n<span class="token operator">|</span>   └── index.html\n├── basics/\n<span class="token operator">|</span>   └── README.md\n├── advenced/\n<span class="token operator">|</span>   └── README.md\n├── _layout.tsx\n├── README.md\n└── pagic.config.ts\n</code></pre>\n<p>At this time, the configuration of <code>pagic.config.ts</code> is also very simple:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Note that if there are files that need to be excluded, you can use <code>exclude</code> to exclude them, for example:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n  exclude<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'examples\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h3 id="scene-3-project--website">Scene 3: Project + Website<a class="anchor" href="#scene-3-project--website">§</a></h3>\n<p>If you want to build a website in a project and you don\'t want to create another repo, then these two methods can meet the needs:</p>\n<ol>\n<li>Place the website source files in the <code>docs</code> directory, and configure <code>srcDir</code> as <code>\'docs\'</code>. The advantage is simple configuration, no need to configure <code>include</code> and <code>exclude</code></li>\n<li>Build the website directly under the root directory, configure <code>srcDir</code> as <code>\'.\'</code>, and then configure <code>include</code> to include the storage directory of the website. The advantage is that it contains <code>README.md</code> in the root directory</li>\n</ol>\n<h3 id="scene-4-only-show-readmemd">Scene 4: Only show <a href="http://README.md">README.md</a><a class="anchor" href="#scene-4-only-show-readmemd">§</a></h3>\n<p>Some projects are very simple. You only need a <code>README.md</code> and no other pages are needed. At this time, you can configure <code>include</code> to include only <code>README.md</code>:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  srcDir<span class="token operator">:</span> <span class="token string">\'.\'</span><span class="token punctuation">,</span>\n  outDir<span class="token operator">:</span> <span class="token string">\'dist\'</span><span class="token punctuation">,</span>\n  include<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'README.md\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<h2 id="plugin">Plugin<a class="anchor" href="#plugin">§</a></h2>\n<p>Plugins are the core function of Pagic, and Pagic even splits the most basic build process into built-in plugins. There are three types of plugins:</p>\n<ol>\n<li>Built-in plugin: the most basic build process, it will run by default</li>\n<li>Official plugin: plugin implemented by Pagic, optional, such as: <code>sidebar</code>, <code>ga</code>, etc.</li>\n<li>Third-party plugins: plugins implemented by third parties, optional. Following Deno\'s design, the entrance is a url, such as: <code>https://github.com/xcatliu/pagic_plugin_example/blob/master/mod.ts</code></li>\n</ol>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>plugins</code></td>\n<td><code>string[]</code></td>\n<td><code>[\'clean\', \'init\', \'md\', \'tsx\', \'script\', \'layout\', \'out\']</code></td>\n<td>The plugin list, the value of the built-in plugin and the official plugin are the plugin name, and the value of the third-party plugin is its entrance url</td>\n</tr>\n</tbody>\n</table></div>\n<p>The order in which the plugins are executed is in the order of their configuration, unless the plugin is implemented with an insert attribute:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  name<span class="token operator">:</span> <span class="token string">\'customPlugin\'</span><span class="token punctuation">,</span>\n  insert<span class="token operator">:</span> <span class="token string">\'before:script\'</span><span class="token punctuation">,</span>\n  <span class="token comment">// The design of fn refers to Deno <a class="token url-link" href="https://deno.land/manual/testing#writing-tests">https://deno.land/manual/testing#writing-tests</a></span>\n  <span class="token function-variable function">fn</span><span class="token operator">:</span> <span class="token punctuation">(</span>pagic<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>\n    <span class="token comment">// balabala</span>\n  <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p><code>name</code> is the unique identifier of the plugin, and it will be used to sort the plugins in <code>insert</code>.</p>\n<p>The syntax of <code>insert</code> is <code>before:${pluginName}</code> or <code>after:${pluginName}</code>. This method is more convenient and more flexible than other static website generators that need to register various life cycle hooks.</p>\n<p><code>fn</code> is a function that only accepts one parameter, an instance of <code>pagic</code>, which can access all the configurations and contexts of Pagic in operation. If the plugin requires some additional configuration, you can add a config item <code>customPlugin</code> in <code>pagic.config.ts</code>, and then you can get the configuration through <code>pagic.config.customPlugin</code> in <code>fn</code>.</p>\n<p>Have to be aware of is:</p>\n<ul>\n<li>User-configured <code>plugins</code> will be appended to the default array instead of replacing it</li>\n<li>When the plugin is running, it will be sorted according to its <code>insert</code> before running</li>\n<li>You can use the <code>-</code> prefix to delete a built-in plugin. This feature is not commonly used, and only needs to be used in the following two situations:\n<ol>\n<li>Just use React as a template engine. When you want to build an html page that does not load React, you can use <code>-script</code> to remove the plugin that builds js scripts. Note that scripts in React such as <code>useState</code> will not be available to execute on the client.</li>\n<li>Want to replace some built-in plugins by yourself, such as using <code>-md</code> to remove the plugin that parses the markdown file, and then add a custom plugin <code>https://github.com/xcatliu/pagic_plugin_custom_md/blob/master/mod.ts</code> This can modify Pagic\'s core construction process to achieve a higher degree of freedom of personalization</li>\n</ol>\n</li>\n<li>Third-party plugins generally require the <code>insert</code> attribute, otherwise it will run after <code>write</code>. Few plugins need to be executed after the build is completed and the file is written</li>\n</ul>\n<h2 id="themes">Themes<a class="anchor" href="#themes">§</a></h2>\n<p>Theme is one of the core functions in Pagic, there are two official themes and lots of third-party themes.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>theme</code></td>\n<td><code>string</code></td>\n<td><code>default</code></td>\n<td>The value of the official theme is the theme name, and the value of the third-party plugin is its entrance url</td>\n</tr>\n</tbody>\n</table></div>\n<p>The operating mechanism of the theme is easy to understand: when you run <code>pagic build</code>, all files in the theme will be &quot;copied&quot; to <code>srcDir</code>, and then run <code>build</code> in the normal mode.</p>\n<p>Of course, this &quot;copy&quot; does not actually copy the file. And when conflicting files are encountered, the user\'s files shall prevail.</p>\n<p>Why does the theme need an entry file?</p>\n<p>Because in Deno’s design, module are based on urls. Imagine a URL represents a theme. We can’t use <code>fs.readdir</code> to find all files in this theme directory like node, so there must be an entry file to indicate which files are included in this topic:</p>\n<pre class="language-ts"><code class="language-ts"><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>\n  files<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">\'assets/index.css\'</span><span class="token punctuation">,</span> <span class="token string">\'assets/reset.css\'</span><span class="token punctuation">,</span> <span class="token string">\'assets/variables.css\'</span><span class="token punctuation">,</span> <span class="token string">\'_layout.tsx\'</span><span class="token punctuation">,</span> <span class="token string">\'favicon.ico\'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">;</span>\n</code></pre>\n<p>Among them, <code>files</code> means files that need to be &quot;copied&quot; to <code>srcDir</code>.</p>\n<h2 id="website-configuration">Website configuration<a class="anchor" href="#website-configuration">§</a></h2>\n<p>Here are some convential configurations, which are usually implemented by plugins or themes.</p>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>title</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>The title of the website, usually placed after the title of the page, such as: <code>Config · Pagic</code>, if there is no page title on the page, only the website title <code>Pagic</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>Website Description, usually displayed in <code>&lt;head&gt;&lt;meta name=&quot;description&quot;&gt;</code>, and may also be displayed on the page</td>\n</tr>\n<tr>\n<td><code>head</code></td>\n<td><code>ReactElement</code></td>\n<td><code>undefined</code></td>\n<td>Additional content injected into <code>&lt;head&gt;</code>, can be written in jsx. Note that you need to change the configuration file to <code>pagic.config.tsx</code></td>\n</tr>\n<tr>\n<td><code>sidebar</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Sidebar</td>\n</tr>\n<tr>\n<td><code>nav</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Navigation on the top</td>\n</tr>\n<tr>\n<td><code>github</code></td>\n<td><code>string</code></td>\n<td><code>undefined</code></td>\n<td>The GitHub address of the website, usually displayed in the upper right corner of the page</td>\n</tr>\n<tr>\n<td><code>ga</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Google Analytics configuration</td>\n</tr>\n<tr>\n<td><code>gitalk</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Gitalk configuration</td>\n</tr>\n<tr>\n<td><code>tocAd</code></td>\n<td><code>ReactNode</code></td>\n<td><code>undefined</code></td>\n<td>Ads displayed above toc</td>\n</tr>\n<tr>\n<td><code>tools</code></td>\n<td></td>\n<td><code>undefined</code></td>\n<td>Some extra features</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id="development-environment-config">Development environment config<a class="anchor" href="#development-environment-config">§</a></h2>\n<div class="table_wrapper"><table>\n<thead>\n<tr>\n<th>Config item</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>watch</code></td>\n<td><code>boolean</code></td>\n<td><code>false</code></td>\n<td>Watch file changes to rebuild.</td>\n</tr>\n<tr>\n<td><code>serve</code></td>\n<td><code>boolean</code></td>\n<td><code>false</code></td>\n<td>Start local service, preview static website.</td>\n</tr>\n<tr>\n<td><code>port</code></td>\n<td><code>number</code></td>\n<td><code>8000</code></td>\n<td>Specify the port of the local service.</td>\n</tr>\n</tbody>\n</table></div>'
        } }),
    'toc': React.createElement("nav", { key: "0", className: "toc" },
        React.createElement("ol", null,
            React.createElement("li", null,
                React.createElement("a", { href: "#design-principles" }, "Design principles")),
            React.createElement("li", null,
                React.createElement("a", { href: "#naming-convention" }, "Naming convention"),
                React.createElement("ol", null,
                    React.createElement("li", null,
                        React.createElement("a", { href: "#file-name-and-directory-name" }, "File name and directory name")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#variables-and-parameters" }, "Variables and parameters")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#config-file-name" }, "Config file name")))),
            React.createElement("li", null,
                React.createElement("a", { href: "#file-related-configuration" }, "File related configuration"),
                React.createElement("ol", null,
                    React.createElement("li", null,
                        React.createElement("a", { href: "#exclude" }, "exclude")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#scenario-1-pure-website-separate-directory-to-store-source-files" }, "Scenario 1: Pure website, separate directory to store source files")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#scenario-2-pure-website-the-root-directory-stores-the-source-files" }, "Scenario 2: Pure website, the root directory stores the source files")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#scene-3-project--website" }, "Scene 3: Project + Website")),
                    React.createElement("li", null,
                        React.createElement("a", { href: "#scene-4-only-show-readmemd" }, "Scene 4: Only show README.md")))),
            React.createElement("li", null,
                React.createElement("a", { href: "#plugin" }, "Plugin")),
            React.createElement("li", null,
                React.createElement("a", { href: "#themes" }, "Themes")),
            React.createElement("li", null,
                React.createElement("a", { href: "#website-configuration" }, "Website configuration")),
            React.createElement("li", null,
                React.createElement("a", { href: "#development-environment-config" }, "Development environment config")))),
    'author': "xcatliu",
    'contributors': [
        "xcatliu"
    ],
    'date': "2020-07-12T00:00:00.000Z",
    'updated': "2020-12-05T13:42:50.000Z",
    'excerpt': "As a senior blogger, I am passionate about to develop blog systems and have written many blog themes. Finally, I couldn't be satisfied with writing themes, and I started writing a blog system. Or in a more gene...",
    'cover': undefined,
    'blog': {
        "isPost": true,
        "posts": [
            {
                "pagePath": "blog/design_pagic_config_ts.md",
                "title": "Design pagic.config.ts",
                "link": "blog/design_pagic_config_ts.html",
                "date": "2020-07-12T00:00:00.000Z",
                "updated": "2020-12-05T13:42:50.000Z",
                "author": "xcatliu",
                "contributors": [
                    "xcatliu"
                ],
                "excerpt": "As a senior blogger, I am passionate about to develop blog systems and have written many blog themes. Finally, I couldn't be satisfied with writing themes, and I started writing a blog system. Or in a more gene..."
            }
        ],
        "categories": [],
        "tags": []
    }
};
